# Metric,Logic Applied
**Mean,We use the formula μ=n∑xi​​. We sum the list and divide by its length.**
**Median,"We sort the list. If the count is even, we find the middle two numbers and average them; if odd, we grab the exact center."**
**Mode,"We use a Dictionary to map each number to its frequency. Then, we extract the key(s) with the highest value."**